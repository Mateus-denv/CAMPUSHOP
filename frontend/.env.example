# Variáveis de ambiente do frontend CampuShop
# Copie este arquivo para .env e ajuste conforme seu ambiente

# URL base da API do backend (deixe vazio para usar URL relativa em produção)
# Em desenvolvimento local sem Docker: http://localhost:8080
# Em produção servido pelo Spring Boot: deixe em branco
VITE_API_URL=
